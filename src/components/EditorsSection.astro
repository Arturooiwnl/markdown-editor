---
import { ArrowRight } from "lucide-react";
---
<section id="editores" class="overflow-hidden bg-transparent py-12 mt-5">
    <div class="relative mx-auto max-w-7xl px-6 py-28 lg:py-24">
        <div class="relative z-10 mx-auto max-w-4xl">
                <h1 class="relative text-balance text-4xl font-semibold md:text-5xl lg:text-6xl text-white text-center font-Audiowide group mb-3">
                    Editores
                    <span class="absolute -bottom-2 w-[60%] sm:w-[40%] left-1/2 -translate-x-1/2 h-2 bg-gradient-to-r from-purple-700/70 to-blue-600/70"></span>
                </h1>

            <p class="py-2 mx-auto max-w-2xl text-center sm:text-left text-base sm:text-xl text-gray-200 leading-relaxed">
                Herramientas de edición para crear y editar archivos Markdown y MDX.
            </p>
            <p class="mx-auto mb-4 max-w-2xl text-center sm:text-left text-base sm:text-xl text-gray-200 leading-relaxed">
            Util si quieres crear contenido de forma rápida y fácil, o si prefieres utilizar una interfaz gráfica para editar tus archivos.
            </p>

            <div class="max-w-6xl w-full mt-16 grid grid-cols-1 md:grid-cols-2 gap-8">
                <a href="/editor" class="group block">
                    <div class="editor-card group relative">
                        <div class="dots-border relative bg-slate-900/50 backdrop-blur-sm p-8">
                            <div class="corner-plus"></div>
                            <div class="corner-plus-right"></div>
                            <div class="flex">
                                <h3 class="text-2xl font-Audiowide mb-6 text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-blue-400">
                                    Editor Markdown
                                </h3>
                                <span class="font-sans mt-1 ml-2 text-gray-200 text-sm h-fit bg-blue-500/20 px-3 py-1 rounded-full">Beta</span>
                                <span class="font-sans mt-1 ml-2 text-gray-200 text-sm h-fit bg-blue-500/20 px-3 py-1 rounded-full">v1.0.1</span>
                            </div>
                            <p class="text-gray-300 mb-6 text-lg">
                                Editor completo con vista previa en tiempo real, soporte para todas las caracteristicas de archivos Markdown y atajos para el teclado.
                            </p>
                            <span class="z-10 inline-flex items-center text-purple-400 group-hover:text-purple-300 transition-all duration-300 text-lg group-hover:scale-105">
                                Disponible ahora <ArrowRight className="ml-2 mt-1 size-5"/>
                            </span>
                            <div class="-z-20 absolute inset-0 bg-gradient-to-r from-purple-600/20 to-blue-600/20 rounded-lg opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                        </div>
                    </div>
                </a>

                <div class="editor-card group relative pointer-events-none">
                    <div class="absolute inset-0 bg-gradient-to-r from-blue-600/20 to-green-600/20 rounded-lg opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                    <div class="dots-border relative bg-slate-900/50 backdrop-blur-sm p-8">
                        <div class="corner-plus"></div>
                        <div class="corner-plus-right"></div>
                        <div class="flex">
                            <h3 class="text-2xl font-Audiowide mb-6 text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-green-400 items-center">
                                Editor MDX 
                            </h3>
                            <span class="font-sans mt-0.5 ml-2 text-gray-200 text-sm h-fit bg-blue-500/20 px-3 py-1 rounded-full">Próximamente</span>
                        </div>
                            
                        <p class="text-gray-300 mb-6 text-lg">
                            Próximamente: Edita archivos MDX con soporte para componentes JSX y elementos HTML.
                        </p>
                        <span class="inline-flex items-center text-blue-400 text-lg">
                            Próximamente
                        </span>
                    </div>
                </div>
            </div>

            <section id="editores" class="mt-24 flex flex-col items-center justify-center text-center">
                <div class="relative inline-block">
                    <h2 class="text-2xl font-sans font-bold text-white">
                        Herramientas 100% Gratuitas
                        <span class="absolute -bottom-2 w-[60%] sm:w-[40%] left-1/2 -translate-x-1/2 h-1.5 bg-gradient-to-r from-purple-700/70 to-blue-600/70"></span>
                    </h2>
                </div>
                    <div class="mt-8 p-6 bg-slate-900/30 max-w-xl backdrop-blur-sm rounded-xl border border-gray-700/30">
                        <p class="text-gray-200 text-lg leading-relaxed">
                            Estas utilidades son completamente gratuitas, puedes utilizarlas tanto como quieras.
                        </p>
                        <a aria-label="ir a la seccion faq" href="#faq" class="text-sm text-sky-500 hover:underline">¿Más dudas? Mira la seccion de FAQ</a>
                    </div>
            </section>
        </div>
    </div>
</section>

<style>
    .dots-border {
        position: relative;
        border: 1px dashed rgba(255, 255, 255, 0.2);
        border-radius: 8px;
    }

    .dots-border::before {
        content: '';
        position: absolute;
        inset: -1px;
        padding: 1px;
        border-radius: 8px;
        background: linear-gradient(90deg, rgba(255,255,255,0.1) 50%, transparent 50%);
        background-size: 4px 1px;
        mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
    }

    .corner-plus::before,
    .corner-plus::after {
        content: '+';
        position: absolute;
        color: rgba(255, 255, 255, 0.5);
        font-size: 1.5rem;
    }
    .corner-plus-right::before,
    .corner-plus-right::after {
        content: '+';
        position: absolute;
        color: rgba(255, 255, 255, 0.5);
        font-size: 1.5rem;
    }

    .corner-plus::before {
        top: -20px;
        left: -8px;
    }

    .corner-plus::after {
        bottom: -15px;
        left: -8px;
    }
    .corner-plus-right::before {
        top: -20px;
        right: -8px;
    }

    .corner-plus-right::after {
        bottom: -15px;
        right: -8px;
    }

    .editor-card {
        cursor: pointer;
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }
</style>

<script is:inline>
    const glowingContainers = document.querySelectorAll('.glowing-container');
    const editorCards = document.querySelectorAll('.editor-card');

    glowingContainers.forEach(container => {
        const glow = container.querySelector('.glow');
        
        container.addEventListener('mousemove', (e) => {
            const rect = container.getBoundingClientRect();
            const x = e.clientX - rect.left - 50;
            const y = e.clientY - rect.top - 50;
            
            if (glow) {
                glow.style.transform = `translate(${x}px, ${y}px)`;
            }
        });
    });

    editorCards.forEach(card => {
        card.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            
            const gradient = card.querySelector('.absolute');
            if (gradient) {
                gradient.style.background = `radial-gradient(circle at ${x}px ${y}px, rgba(139, 92, 246, 0.2), transparent 50%)`;
            }
        });
    });
</script>